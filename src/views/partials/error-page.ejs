<section class="panel narrow">
  <h1><%= heading %></h1>
  <p class="muted"><%= message %></p>
  <a class="button-link" href="<%= (typeof basePath !== 'undefined' && basePath) ? `${basePath}/dashboard` : '/' %>">Back to dashboard</a>
</section>
