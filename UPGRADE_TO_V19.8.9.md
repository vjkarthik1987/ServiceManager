# Upgrade to v19.8.9

1. Copy your existing `.env` into this build.
2. Run `npm install` if dependencies are not already installed.
3. Start with `npm.cmd start`.

No database migration or re-provisioning is required for this UI/ownership release.
