Service Desk v24.1.0 — Build Validation

Result: PASS

Regression suite:
- 97 tests
- 97 passed
- 0 failed

Validated:
- v24 configuration validation PASS
- JavaScript/MJS syntax checks PASS for changed runtime/model files
- Incident creation lifecycle/RCA exclusion for Customer, Partner and Agent
- Components visible through service_context on all four Incident subtype forms
- Compact Change status control beside the current stage
- Support routing remains separate from workflow status changes
- Configurable SLA clock-start triggers:
  * Issue raised (default for newly created/seeded policies)
  * Severity selected
  * Priority selected
  * Received by L2
  * Received by L3
  * Specific workflow status
- Existing SLA startedAt is preserved after the clock starts
- SLA eligibility remains separate from SLA clock-start configuration

Compatibility note:
- Existing SLA policies keep their stored clockStartTrigger until an administrator changes it.
- New and newly seeded SLA policies default to ticket_created / Issue raised.
