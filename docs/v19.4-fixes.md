# v19.4 fixes

- Administrators can make an audited direct status override when a transition is not present in the configured workflow graph.
- Agent and client status changes continue to enforce configured transitions.
- Admin status dropdowns label non-configured destinations as `admin override`.
- Support-stage and movement-rule tables were replaced by responsive configuration cards for clearer alignment and less visual crowding.
